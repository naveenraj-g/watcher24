/**
 * docs-config.ts — shared constants for console documentation pages.
 * Change values here to update them across all MDX docs that import this file.
 */

/** Production gateway ingest URL. Used in code examples and prose across all SDK docs. */
export const GATEWAY_URL = "https://ingest.watcher24.io";
