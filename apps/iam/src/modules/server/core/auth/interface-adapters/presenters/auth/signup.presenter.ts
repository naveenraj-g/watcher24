// Use an inline presenter for simple output mapping.
// Extract a dedicated presenter when presentation logic grows in complexity.

/**
 * Presenter responsible for transforming use case output
 * into a client-facing response model.
 *
 * This layer handles formatting, mapping, and data shaping,
 * without containing business rules or infrastructure logic.
 */
